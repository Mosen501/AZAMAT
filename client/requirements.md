## Packages
framer-motion | For fluid animations and page transitions
lucide-react | For high-tech UI icons
react-syntax-highlighter | For displaying the raw JSON in the admin panel

## Notes
- `Orbitron` and `Rajdhani` Google fonts used for a cyberpunk, high-tech dashboard aesthetic.
- Relies on `localStorage` for run history (no backend state required for active runs).
- Uses `api.scenario.get.path` and `api.debrief.generate.path` to communicate with the backend.
